

bool isPosInt(string userInput){
    //loop through each character in userInput
        //if character ascii value is not between 48 and 57, return false
    int asciiVal = (int)userInput[0];
    //once loop is exited, we know that all characters were valid so we can return true

}



    bool isPosInt(string pass_length)  {
        int assiVal = (int)pass_length[0];
        for (int i=0; i<userInput.length();i++ ){
            (int)userInput[i];
        }